# Application Name
spring.application.name=SpringEmployeePayrollApp


# Logging Configuration
logging.level.root=INFO
logging.file.name=logs/dev.log
logging.level.com.employee=DEBUG

# Console Log Format
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# File Log Format
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Database Configuration (Use Environment Variables, with Default Fallback)
spring.datasource.url=jdbc:mysql://localhost:3306/payroll_service?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Omyadav@124
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
